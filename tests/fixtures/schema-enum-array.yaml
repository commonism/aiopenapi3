openapi: "3.1.0"
info:
  version: 1.0.0
  title: enum with complex type - not supported

components:
  schemas:
    obj:
      type: array
      items:
        type: integer
      enum:
        - [1,2,3]
