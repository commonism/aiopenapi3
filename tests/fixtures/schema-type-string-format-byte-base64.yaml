openapi: "3.1.0"
info:
  version: 1.0.0
  title: OpenAPI schema where the type is base64

components:
  schemas:
    Base64Root:
      type: string
      format: byte
    Base64Property:
      type: object
      properties:
        data:
          type: string
          format: byte
