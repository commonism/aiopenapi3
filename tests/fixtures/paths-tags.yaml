openapi: 3.0.3
info:
  title: ''
  version: 0.0.0
servers:
  - url: http://127.0.0.1/api

security:
  - {}

paths:
  /user/:
    get:
      operationId: list
      tags: ["users"]
      responses:
        "200":
          description: "ok"
          content:
            application/json:
              schema:
                type: string
                enum:
                  - list
  /item/:
    get:
      operationId: list
      tags: ["items", "objects"]
      responses:
        "200":
          description: "ok"
          content:
            application/json:
              schema:
                type: string
