openapi: "3.1.0"
info:
  version: 1.0.0
  title: OpenAPI schema where the type is missing

components:
  schemas:
    Any:
      properties:
        id:
          type: integer
    Ref:
      type: object
      allOf:
        - $ref: "#/components/schemas/Any"
