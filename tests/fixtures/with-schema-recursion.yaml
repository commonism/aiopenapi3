openapi: "3.0.0"
info:
  version: 1.0.0
  title: Any OpenAPI schema with recursion

components:
  schemas:
    B:
      type: object
      properties:
        ofB:
          type: string
        a:
          $ref: '#/components/schemas/A'

    A:
      type: object
      properties:
        ofA:
          type: integer
        b:
          $ref: '#/components/schemas/B'

    C:
      type: object
      properties:
        a:
          $ref: '#/components/schemas/A'
