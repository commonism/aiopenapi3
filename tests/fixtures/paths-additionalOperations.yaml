openapi: 3.2.0
info:
  title: query Example
  version: 1.0.0

servers:
  - url: /

paths:
  /api/data:
    additionalOperations:
      test:
        operationId: test
        responses:
          "200":
            content:
              application/json:
                schema:
                  type: string
                  const: ok
