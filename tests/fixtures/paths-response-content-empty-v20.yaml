swagger: "2.0"
info:
  title: with empty response
  description: with empty response
  version: 1.0.0
host: api.example.com
basePath: /v1
schemes:
  - https

consumes:
  - application/json
produces:
  - application/json

definitions: {}

paths:
  /empty:
    get:
      operationId: empty
      responses:
        "200":
          description: OK
  /headers:
    get:
      operationId: headers
      responses:
        "200":
          description: OK
          headers:
            X-required:
              type: string
